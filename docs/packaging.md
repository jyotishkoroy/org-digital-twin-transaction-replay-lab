# Packaging notes

This repository ships as source. For production distribution, consider:

- Unlocked package with versioning
- GitHub releases with tagged versions
- Signed packages (if distributing widely)

This repo intentionally keeps packaging lightweight so it can be deployed to any org with CLI.
